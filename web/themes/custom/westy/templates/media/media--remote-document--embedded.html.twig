{#
/**
 * @file
 * Template implementation for the media_remote_document theme hook.
 *
 * Available variables:
 * - document_id: (string) The document ID.
 * - slug: (string) The document slug.
 */
#}

TEST TEST
{% embed 'media.html.twig' with {
  'margin' : '0'
}%}
  {% spaceless %}
    {% block status %}
      {% set badge_classes = badge_classes|merge(['d-inline-block', 'mr-2']) %}
      {{ parent() }}
    {% endblock %}
    {% block content %}
      {{ content }}
    {% endblock %}
  {% endspaceless %}
{% endembed %}