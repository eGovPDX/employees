{#
/**
 * @file
 * Westy Layout for 3 columns with menu, side, and main regions
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
    'layout',
    'layout--westy-three-column',
    'row',
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {% if content.main %}
      <div {{ region_attributes.main.addClass('layout__region', 'layout__region--main', 'col-12', 'col-lg-8', 'col-xl-6', 'order-1', 'order-lg-2') }}>
        {{ content.main }}
      </div>
    {% endif %}
    {% if content.side %}
      <div {{ region_attributes.side.addClass('layout__region', 'layout__region--side', 'col-12', 'col-lg-4', 'col-xl-3', 'order-3') }}>
        {{ content.side }}
      </div>
    {% endif %}
    {% if content.menu %}
      <div {{ region_attributes.menu.addClass('layout__region', 'layout__region--menu', 'col-12', 'col-md-8', 'col-xl-3', 'order-2', 'order-lg-3', 'order-xl-1') }}>
        {{ content.menu }}
      </div>
    {% endif %}
  </div>
{% endif %}
